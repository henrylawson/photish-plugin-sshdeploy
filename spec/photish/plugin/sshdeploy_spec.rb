require 'spec_helper'

describe Photish::Plugin::Sshdeploy do
  it 'has a version number' do
    expect(Photish::Plugin::Sshdeploy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
